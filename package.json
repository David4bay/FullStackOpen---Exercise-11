{
  "name": "phonebook",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "cross-env NODE_ENV=production node index.js",
    "server": "cross-env NODE_ENV=development nodemon app.js",
    "test": "cross-env NODE_ENV=test jest --config jest.config.js --verbose --runInBand ./__test__",
    "lint": "eslint --fix ./**/*.{js}",
    "build": "npm run lint && npm run test && npm run test:e2e",
    "cypress:open": "cypress open"
  },
  "jest": {
    "testEnvironment": "node"
  },
  "author": "David Bayode",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "mongoose": "^8.0.3",
    "morgan": "^1.10.0",
    "nodemon": "3.0.2"
  },
  "devDependencies": {
    "cypress": "12.0.0",
    "@types/node": "^20.14.12",
    "cross-env": "^7.0.3",
    "eslint": "^8.56.0",
    "jest": "^29.7.0",
    "supertest": "^7.0.0"
  }
}
